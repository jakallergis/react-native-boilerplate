/**
 * @flow
 *
 * The action creators for changing the TabletNavigator state.
 *
 * @Note: You would notice that in the root actions file there are
 * action creators that relate directly to the TabletNavigator's
 * state. The reason those action creators are in the root and not
 * in here is because the same action creators are directly related
 * to the MobileNavigator's state as well since some screens and their
 * names are the same for both navigators.
 */